#include <stdio.h>
#include <wingdi.h>

// 初始化，建立一个空的线性表L
// void InitList(*L){

// }

// 判断线性表是否为空，空-->ture 非空-->false
// void ListEmpty(L){

// }

// 将线性表清空
// ClearList(*L){

// }

// 将线性表L中的第i个元素值返回给e
Status GetElem(SqList, int i, ELemType *e){
    if (L.length==0 || i<1 || i>L.length){
        return ERROR;
    }
    e = L.data{i-1};
}
// }

// 在线性表L中查找与给定值e相等的元素
// 如果查找成功，返回元素在表中序号 否则，返回0
// LocateElem(L, e){

// }

// 
